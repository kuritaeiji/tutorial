<%= form_with(model: @micropost, local: true) do |f| %>
  <%= render('shared/error_messages', object: @micropost) %>

  <%= f.text_area(:content, placeholder: '140字以内', class: 'form-control mb-2') %>
  <%= f.file_field(:image) %>
  <%= f.submit('投稿', class: 'btn btn-primary w-100') %>
<% end %>