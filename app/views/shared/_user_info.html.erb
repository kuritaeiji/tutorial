<div class="d-flex">
  <div class="mr-3"><%= gravatar_for(current_user) %></div>

  <div>
    <h3 class="mb-0"><%= current_user.name %></h3>
    <%= link_to('プロフィール', current_user, class: 'd-block') %>
    <p class="d-block"><%= current_user.microposts.count %>投稿</p>
  </div>
</div>